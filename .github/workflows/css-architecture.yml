name: CSS Architecture

on:
  push:
  pull_request:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  architecture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Guard CSS architecture
        run: python3 .github/ci/css-architecture-guard.py
